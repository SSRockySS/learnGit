字面值(形如"xxx")不是string类型
string::size_type 是一个无符号类型
vector<Type> vec{} 列表初始化要求每一个对象都是正确的类型，如vector<string>{10, "hi"}其实是构造初始化…
//跳过了数组与指针部分，很关键
constexpr修饰的常量表达式，可在编译过程中直接获取表达式结果；表达式右侧也应该是常量或constexpr函数。
在大多数表达式中，使用数组类型对象其实是使用一个指向该数组首元素的指针。（重要）
