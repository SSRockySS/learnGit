关于const，还是有一堆问题……
递归6.33写错了……回去再看看。
返回数组指针的方法，以及C++11的尾置返回类型，使用decltype推断返回数组的类型
内联函数：使用inline修饰符;
函数在声明时可设置默认值（默认实参），调用含有默认实参的函数时，可以省略相应的实参。
在调用包含多个重载的函数时，编译器首先选择可行函数，然后在可行函数列表中选择最佳匹配函数，若无法判断可行函数优劣，则报告二义性错误。
关于类型转换：数组或函数转对应的指针类型、多种情况下的const转换、类型提升、算数类型转换、类类型转换 等。（重要）
函数指针，以及返回函数指针的方法（与数组指针类似）
